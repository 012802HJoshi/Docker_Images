# 🗄️ MongoDB Shared Container Setup (Production Ready)

This project uses a **single MongoDB container with persistent storage**, connected to multiple Node.js applications through a **shared Docker network**. The setup is secure, scalable, and production-ready.

